# vite react

React is a **Library**.

Angular is a **Framework**.

SPA - **Single Page Application**

JSX - **Javascript XML**

## Content

### Component

- Reusable
- self-contained

### Props, default-props, propTypes

- props - property
- propTypes - condition to render the type(int, str, bool)

### Dynamic content loading (map)

- `array.map( (value, index) => {condition /*key={index}*/ })`

### Filtering (`filter()`) and sorting (`sort()`)

- ...

## Steps to create

`npm create vite@latest`
to create a new project `...`
`npm run dev`
and to run development server
# Vite react

Create a new vite react app
`npm create vite@latest`

...

Run development version
`npm run dev`
