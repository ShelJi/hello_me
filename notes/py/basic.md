# Py

[py](https://stackoverflow.com/questions/1278705/when-i-catch-an-exception-how-do-i-get-the-type-file-and-line-number)

important for python for finding errors in a line specifically

```bash
python -m venv venv

venv/scripts/activate

python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
pip list
```

## Pyenv

pyenv is a Python version management tool that allows you to easily switch between multiple versions of Python on your system. It is especially useful when working on projects that require different Python versions.

```bash
pyenv install --list
Install a specific Python version

pyenv install 3.9.9
Set a global Python version

pyenv global 3.9.9
Set a Python version for a specific project

pyenv local 3.9.9
Check the currently active Python version

pyenv version
Uninstall a Python version

pyenv uninstall 3.9.9
```
